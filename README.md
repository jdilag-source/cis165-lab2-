# cis165-lab2-
#include <iostream>

int main() 
{
    std::cout<<"Miles per Gallon\n";
    int num1= 312;
    int num2= 16;
    int mpg= num1/num2;
    std::cout<<"The mpg is :"<<mpg<<"\n";

    return 0;
}
#include <iostream>

int main()
{
    std::cout<<"Miles per Gallon\n";
    int num1= 320;
    int num2= 80;
    int mpg= num1/num2;
    std::cout<<"The mpg is :"<<mpg<<"\n";

    return 0;
}
#include <iostream>

int main()
{
    std::cout<<"Sum of 2 #'s\n";
    int num1= 100;
    int num2= 50;
    int sum= num1+num2;
    std::cout<<"The sum is : "<<sum<<"\n";

    return 0;
}
#include <iostream>

int main()
{
    std::cout<<"Sum of 2 #'s\n";
    int num1= 130;
    int num2= 70;
    int sum= num1+num2;
    std::cout<<"The sum is : "<<sum<<"\n";

    return 0;
}
For sum.cpp: explain how the starting values move through your calculation into total and then to the output. Why store the calculation in total before printing?
-The starting values are placed in the code with "int" and then us the coders place a value on them. After the first value I do the same thing for the second value. After getting two values, I then add them up and that is basically all the steps I did to complete this part of the lab.

For mpg.cpp: explain the formula, the data types you chose, and what can happen if C++ performs division using two integer operands. Trace your changed-value test from the values through the result.
-So for this formula I honestly did the same thing for my sum of two numbers. I copy and pasted my code? and just switched up some signs. I had two values one being 312 and then the other being 16. Instead of adding them up I changed the sign to a division sign and divided 312 by 16 and that is how I was able to complete this part of the lab. 
